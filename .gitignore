*.log
.DS_Store
/build
.env
.env.backup
.env.production
.phpactor.json
.phpunit.result.cache
/.fleet
/.idea
/.nova
/.phpunit.cache
/.vscode
/.zed
/auth.json
/node_modules
/public/build
/public/hot
/public/storage
/storage/*.key
/storage/pail
/vendor
Homestead.json
Homestead.yaml
Thumbs.db
/Caddyfile
/Caddyfile.lan

# Agent worktrees and local Claude Code state — never project code.
/.claude/

# Output of scripts/audit/ui-audit.mjs
/audit-report.json

# Binaries the Windows setup script downloads (caddy and friends). Tens of
# megabytes, per-machine, and re-fetchable — the script is the source of truth.
/scripts/windows/bin/

/*.png
/*.jpg
/*.jpeg
